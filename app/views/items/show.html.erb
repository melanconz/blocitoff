<h1><%= @item.name %> by <%= @item.date %> or else!!</h1>
  <div class="row">
    <div class="col-md-4">
      <%= link_to "Delete Item", [@item.user, @item], method: :delete, class: 'btn btn-danger', data: { confrim: 'Are you sure you want to delete that item?'} %>
    </div>
  </div>
